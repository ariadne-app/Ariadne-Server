Flask==2.2.3  # Flask for building the web app
flask-cors==3.0.10  # Flask-CORS for handling cross-origin requests
opencv-python-headless==4.8.0.74  # OpenCV for image processing
numpy==1.24.3  # NumPy for numerical computations
ultralytics==8.0.36  # YOLO model from ultralytics
